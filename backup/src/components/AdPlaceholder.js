import React from "react";

function AdPlaceholder() {
  return (
    <div
      style={{
        width: "100%",
        minHeight: 90,
        background: "#f3f3f3",
        border: "1.5px dashed #bbb",
        borderRadius: 8,
        margin: "24px 0",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#888",
        fontSize: 16,
      }}
    >
      광고 자리 (Ad Placeholder)
    </div>
  );
}

export default AdPlaceholder; 